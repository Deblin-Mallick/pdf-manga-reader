{
  "name": "pdf-manga-reader-monorepo",
  "version": "1.0.0",
  "description": "Monorepo for PDF and Manga Reader with FastAPI backend",
  "scripts": {
    "install:frontend": "cd frontend && npm install",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"cd backend && .venv\\Scripts\\python -m uvicorn app.main:app --reload --port 8000\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@subframe/core": "^1.155.0"
  }
}
